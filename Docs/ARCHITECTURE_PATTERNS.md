# Архитектурные паттерны для Unreal Engine

## Как добавлять логику "снаружи" персонажа

---

## 🎯 Подход 1: ActorComponent (самый простой и популярный)

### Что это:
Вы создаете `UActorComponent`, который содержит всю логику системы.
Персонаж просто добавляет компонент и вызывает его методы.

### Преимущества:
✅ Легко добавлять/удалять функциональность
✅ Переиспользуемость (один компонент на разных персонажах)
✅ Изолированность (изменения в одном компоненте не влияют на другие)
✅ Можно добавлять компоненты из Blueprint
✅ Стандартный подход Unreal Engine

### Примеры в вашем проекте:
- `UInteractionComponent` - взаимодействие с объектами
- `USpringArmComponent` - камера
- `UCameraComponent` - камера

### Когда использовать:
- Любая игровая механика (спринт, прыжки, dash, climb)
- UI логика (crosshair, health bar)
- Эффекты (particles, sounds)
- Любая логика, которая может быть на разных персонажах

### Пример создания:
```cpp
// MyFeatureComponent.h
UCLASS(ClassGroup = (Custom), meta = (BlueprintSpawnableComponent))
class UMyFeatureComponent : public UActorComponent {
  GENERATED_BODY()

public:
  UFUNCTION(BlueprintCallable)
  void DoFeature();

private:
  UPROPERTY()
  ACharacter *OwnerCharacter;
};

// В персонаже:
UPROPERTY(VisibleAnywhere, BlueprintReadOnly)
UMyFeatureComponent *FeatureComponent;
```

---

## 🎯 Подход 2: Interface (для полиморфизма)

### Что это:
Вы создаете интерфейс с методами, которые может реализовать любой класс.
Внешние системы работают через интерфейс, не зная конкретный класс.

### Преимущества:
✅ Полная независимость от конкретного класса
✅ Можно вызывать методы на любом Actor, который реализует интерфейс
✅ Blueprint может реализовывать интерфейсы
✅ Удобно для систем вроде "Damageable", "Interactable"

### Примеры в вашем проекте:
- `IInteractableInterface` - любой объект может быть интерактивным

### Когда использовать:
- Когда одну и ту же функциональность нужно на разных типах Actor
- Для событий (Damageable, Destroyable, Highlightable)
- Для проверок типа "а может ли этот объект X?"

### Пример создания:
```cpp
// MyInterface.h
UINTERFACE(BlueprintType)
class UMyInterface : public UInterface {
  GENERATED_BODY()
};

class IMyInterface {
  GENERATED_BODY()

public:
  UFUNCTION(BlueprintNativeEvent, BlueprintCallable)
  void DoSomething();
};

// Персонаж реализует:
class AMyCharacter : public ACharacter, public IMyInterface {
  virtual void DoSomething_Implementation() override;
};

// Использование:
if (Actor->Implements<UMyInterface>()) {
  IMyInterface::Execute_DoSomething(Actor);
}
```

---

## 🎯 Подход 3: Gameplay Ability System (GAS)

### Что это:
Официальная система Unreal для игровых способностей, атрибутов и эффектов.
Самая мощная, но и самая сложная система.

### Преимущества:
✅ Полная модульность (способности как отдельные классы)
✅ Сетевая репликация из коробки
✅ Атрибуты (HP, Stamina) с автоматическими расчетами
✅ Эффекты (buffs, debuffs) со стаками и временем
✅ Prediction для мультиплеера
✅ Используется в Fortnite, Paragon

### Когда использовать:
- Мультиплеер игры с сетевой синхронизацией
- RPG с множеством способностей и атрибутов
- Сложные системы баффов/дебаффов
- Если нужна готовая система для способностей

### Пример:
```cpp
// Способность спринта как отдельный класс
UCLASS()
class UGameplayAbility_Sprint : public UGameplayAbility {
  GENERATED_BODY()

public:
  virtual void ActivateAbility(...) override {
    // Логика спринта здесь
  }
};

// Персонаж:
UPROPERTY()
UAbilitySystemComponent *AbilitySystemComponent;
```

### Внимание:
⚠️ Очень большая система, требует изучения
⚠️ Много boilerplate кода
⚠️ Оверкилл для простых игр

---

## 🎯 Подход 4: Blueprint Function Library (утилиты)

### Что это:
Статические функции, которые можно вызывать откуда угодно.
Для логики без состояния.

### Преимущества:
✅ Простейший способ для helper функций
✅ Доступны везде (Blueprint и C++)
✅ Нет необходимости хранить ссылки

### Когда использовать:
- Математические расчеты
- Конвертеры данных
- Утилиты без состояния
- Хелперы для работы с Engine API

### Пример:
```cpp
UCLASS()
class UMyBlueprintLibrary : public UBlueprintFunctionLibrary {
  GENERATED_BODY()

public:
  UFUNCTION(BlueprintCallable, Category = "Movement")
  static float CalculateSprintSpeed(ACharacter *Character, float BaseSpeed);
};
```

---

## 🎯 Подход 5: Subsystem (глобальные системы)

### Что это:
Singleton-подобные системы, живущие на уровне World, Game Instance или Engine.
Для глобальной логики.

### Преимущества:
✅ Один экземпляр на весь мир/игру
✅ Автоматическое создание и уничтожение
✅ Доступны через статические геттеры
✅ Идеально для менеджеров (SaveManager, QuestManager)

### Когда использовать:
- Менеджеры (Audio, Save, Quest, Dialogue)
- Глобальные системы (Weather, Time Of Day)
- Координаторы между Actor

### Пример:
```cpp
UCLASS()
class UMyWorldSubsystem : public UWorldSubsystem {
  GENERATED_BODY()

public:
  virtual void Initialize(FSubsystemCollectionBase &Collection) override;

  UFUNCTION(BlueprintCallable)
  void DoGlobalThing();
};

// Использование:
UMyWorldSubsystem *Subsystem = GetWorld()->GetSubsystem<UMyWorldSubsystem>();
Subsystem->DoGlobalThing();
```

---

## 🎯 Подход 6: Data-Driven (Data Assets + Tables)

### Что это:
Логика описывается через данные (DataAsset, DataTable), а не код.
Designers могут менять поведение без программирования.

### Преимущества:
✅ Designers работают независимо
✅ Легко балансировать (скорость, урон, дистанция)
✅ Можно менять без перекомпиляции
✅ Легко создавать варианты (разные персонажи с разными параметрами)

### Когда использовать:
- Настройки персонажей (скорость, HP, урон)
- Конфигурации оружия
- Параметры способностей
- Любые числовые настройки

### Пример:
```cpp
// CharacterData.h
UCLASS(BlueprintType)
class UCharacterDataAsset : public UDataAsset {
  GENERATED_BODY()

public:
  UPROPERTY(EditAnywhere, BlueprintReadOnly)
  float WalkSpeed = 500.0f;

  UPROPERTY(EditAnywhere, BlueprintReadOnly)
  float SprintSpeed = 800.0f;

  UPROPERTY(EditAnywhere, BlueprintReadOnly)
  int32 MaxJumps = 2;
};

// В персонаже:
UPROPERTY(EditAnywhere, BlueprintReadOnly)
UCharacterDataAsset *CharacterData;
```

---

## 📊 Сравнительная таблица

| Подход | Сложность | Модульность | Мультиплеер | Переиспользование | Для чего |
|--------|-----------|-------------|-------------|-------------------|----------|
| **ActorComponent** | ⭐ Легко | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | Игровые механики |
| **Interface** | ⭐⭐ Средне | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | Полиморфизм |
| **GAS** | ⭐⭐⭐⭐⭐ Сложно | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | AAA мультиплеер |
| **BP Library** | ⭐ Легко | ⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | Утилиты |
| **Subsystem** | ⭐⭐ Средне | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ | Глобальные менеджеры |
| **Data-Driven** | ⭐ Легко | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | Конфигурации |

---

## 💡 Рекомендации для вашего проекта

### Для управления (Movement, Sprint, Jump):
✅ **ActorComponent** - идеальный вариант
- Создайте компоненты: `USprintComponent`, `UJumpControlComponent`, `UClimbComponent`
- Персонаж просто делегирует вызовы

### Для взаимодействия (уже используете):
✅ **ActorComponent** + **Interface**
- `UInteractionComponent` на персонаже
- `IInteractableInterface` на объектах мира

### Для настроек персонажа:
✅ **Data Assets**
- Создайте `UCharacterDataAsset` с параметрами
- Разные персонажи = разные DataAssets

### Для глобальных систем (сохранения, квесты):
✅ **Subsystem**
- `USaveGameSubsystem`
- `UQuestManagerSubsystem`

---

## 🔄 Как перейти к модульной архитектуре

### Шаг 1: Определите системы
Разбейте логику персонажа на независимые системы:
- Movement (Sprint, Crouch, Prone)
- Jump (Single, Double, Dash)
- Interaction (Pickup, Use, Talk)
- Combat (Attack, Block, Dodge)
- Inventory
- etc.

### Шаг 2: Создайте компоненты
Для каждой системы создайте отдельный `UActorComponent`:
```
USprintComponent
UJumpControlComponent
UCombatComponent
UInventoryComponent
```

### Шаг 3: Перенесите логику
Переместите код из персонажа в компоненты:
- UPROPERTY переменные → в компонент
- Функции логики → в компонент
- Input handlers в персонаже → просто вызывают методы компонента

### Шаг 4: Персонаж становится "контейнером"
Персонаж теперь:
- Хранит компоненты
- Делегирует Input в компоненты
- Предоставляет геттеры для внешних систем

### Шаг 5: Blueprint Extension
В Blueprint можете:
- Переопределять компоненты (Replace Component Class)
- Добавлять новые компоненты
- Биндить события компонентов

---

## ✅ Результат

### Было (монолитный персонаж):
```cpp
class ACharacter {
  void HandleSprint();      // 50 строк
  void HandleJump();        // 70 строк
  void HandleCombat();      // 200 строк
  void HandleInventory();   // 150 строк
  // ... 1000+ строк в одном файле
};
```

### Стало (модульный персонаж):
```cpp
class ACharacter {
  USprintComponent *Sprint;
  UJumpComponent *Jump;
  UCombatComponent *Combat;
  UInventoryComponent *Inventory;

  void OnSprintPressed() { Sprint->Start(); }
  void OnJumpPressed() { Jump->TryJump(); }
  // ... ~50 строк делегатов
};
```

### Преимущества:
✅ Каждая система в отдельном файле (легко найти)
✅ Изменения изолированы (правки спринта не сломают прыжки)
✅ Легко добавлять новые системы (просто новый компонент)
✅ Легко убирать системы (удалить компонент)
✅ Переиспользование (компоненты на разных персонажах)
✅ Blueprint friendly (можно менять компоненты в BP)
✅ Тестирование (каждый компонент тестируется отдельно)

---

## 📚 Дополнительные ресурсы

- [Unreal Engine: Actor Components](https://docs.unrealengine.com/5.3/en-US/components-in-unreal-engine/)
- [Gameplay Ability System](https://docs.unrealengine.com/5.3/en-US/gameplay-ability-system-for-unreal-engine/)
- [Blueprint Interface](https://docs.unrealengine.com/5.3/en-US/blueprint-interface-in-unreal-engine/)
- [Programming Subsystems](https://docs.unrealengine.com/5.3/en-US/programming-subsystems-in-unreal-engine/)
