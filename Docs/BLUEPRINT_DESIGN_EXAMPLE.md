# Пример проектирования Blueprint

## Задача: Sprint System Blueprint

Создадим Blueprint компонент для системы спринта.

---

## 📐 Архитектура Blueprint

### **BP_SprintComponent** (Actor Component Blueprint)

---

## 🎯 Variables (Переменные)

```
┌────────────────────────────────────────────────────────┐
│ VARIABLES                                              │
├────────────────────────────────────────────────────────┤
│ WalkSpeed          │ Float  │ 500.0   │ Editable      │
│ SprintSpeed        │ Float  │ 800.0   │ Editable      │
│ bIsSprinting       │ Bool   │ False   │ Read-Only     │
│ OwnerCharacter     │ AChar* │ None    │ Private       │
│ MovementComponent  │ UChar* │ None    │ Private       │
│ MinStamina         │ Float  │ 10.0    │ Editable      │
└────────────────────────────────────────────────────────┘
```

---

## 🔧 Function: BeginPlay (Event)

### Схема нод:

```
┌──────────────────┐
│ Event BeginPlay  │
└────────┬─────────┘
         │ Exec
         ▼
┌──────────────────┐
│   Get Owner      │
└────────┬─────────┘
         │ AActor*
         ▼
┌──────────────────────────┐
│ Cast to Character        │
└────────┬─────────────────┘
         │ Success?
         │ Yes          No
         ▼              │
┌──────────────────┐    │
│ Set              │    │
│ OwnerCharacter   │    │
└────────┬─────────┘    │
         │              │
         ▼              │
┌──────────────────────┐ │
│ Get Character        │ │
│ Movement             │ │
└────────┬─────────────┘ │
         │ UCharMove*    │
         ▼              │
┌──────────────────┐    │
│ Set              │    │
│ MovementComponent│    │
└────────┬─────────┘    │
         │              │
         ▼              │
┌──────────────────┐    │
│ Set Max Walk     │    │
│ Speed            │    │
│ (WalkSpeed)      │    │
└────────┬─────────┘    │
         │              │
         ▼              ▼
         └──────┬───────┘
                │
                ▼
            [ END ]
```

### Пошаговые инструкции:

1. **Event BeginPlay**
   - Drag from Events panel → BeginPlay

2. **Get Owner**
   - Right click → Get Owner
   - Connect: BeginPlay Exec → Get Owner Exec

3. **Cast to Character**
   - Drag from Get Owner return → Cast to Character
   - Connect execution pins

4. **SET OwnerCharacter**
   - Drag OwnerCharacter variable → SET
   - Connect: Cast success Exec → SET Exec
   - Connect: As Character → OwnerCharacter input

5. **Get Character Movement**
   - Drag from OwnerCharacter → Get Character Movement
   - Connect execution

6. **SET MovementComponent**
   - Drag MovementComponent variable → SET
   - Connect: Return Value → MovementComponent input

7. **Set Max Walk Speed**
   - Drag from MovementComponent → Set Max Walk Speed
   - Drag WalkSpeed variable → GET → connect to New Max Walk Speed
   - Connect execution

---

## 🔧 Function: StartSprint (Custom Function)

### Inputs: None
### Outputs: None

### Схема нод:

```
                    [ ENTRY ]
                        │
                        ▼
        ┌───────────────────────────────┐
        │ Branch                        │
        │ Condition: Is Valid           │
        │ (MovementComponent)           │
        └───────┬──────────────┬────────┘
                │ True         │ False
                ▼              ▼
    ┌─────────────────┐   [ RETURN ]
    │ Get Stamina     │
    │ Component       │
    └────────┬────────┘
             │ Optional
             ▼
    ┌─────────────────────┐
    │ Branch: Is Valid?   │
    └────────┬────────────┘
             │ True
             ▼
    ┌─────────────────────┐
    │ Has Enough Stamina  │
    │ (MinStamina)        │
    └────────┬────────────┘
             │ Bool
             ▼
    ┌─────────────────────┐
    │ Branch              │
    └────────┬────────────┘
             │ True
             ▼
    ┌─────────────────────┐
    │ Is Falling?         │
    │ (MovementComponent) │
    └────────┬────────────┘
             │ Bool
             ▼
    ┌─────────────────────┐
    │ Branch              │
    └────────┬────────────┘
             │ False (not falling)
             ▼
    ┌─────────────────────┐
    │ SET bIsSprinting    │
    │ = True              │
    └────────┬────────────┘
             │
             ▼
    ┌─────────────────────┐
    │ Set Max Walk Speed  │
    │ (SprintSpeed)       │
    └────────┬────────────┘
             │
             ▼
         [ RETURN ]
```

### Детальное подключение:

```
NODE 1: Entry
├─ Exec Out ──────────────► NODE 2

NODE 2: IsValid (MovementComponent)
├─ Object to Test ◄────── MovementComponent (GET)
├─ Is Valid ──────────────► NODE 3
└─ Is Not Valid ──────────► Return Node

NODE 3: Find Component By Class (UStaminaComponent)
├─ Target ◄──────────────── Get Owner
├─ Return Value ──────────► NODE 4 (Object pin)
└─ Exec ──────────────────► NODE 4 (Exec pin)

NODE 4: IsValid (StaminaComponent)
├─ Object to Test ◄────── NODE 3 Return Value
├─ Is Valid ──────────────► NODE 5
└─ Is Not Valid ──────────► NODE 7 (skip stamina check)

NODE 5: Has Enough Stamina (StaminaComponent function)
├─ Target ◄──────────────── Stamina Component reference
├─ Amount ◄──────────────── MinStamina (GET)
├─ Return Value ──────────► NODE 6 (Condition)

NODE 6: Branch
├─ Condition ◄──────────── NODE 5 Return Value
├─ True ──────────────────► NODE 7
└─ False ─────────────────► Return Node

NODE 7: IsFalling (MovementComponent function)
├─ Target ◄──────────────── MovementComponent (variable)
├─ Return Value ──────────► NODE 8 (Condition)

NODE 8: Branch
├─ Condition ◄──────────── NODE 7 Return Value
├─ False ─────────────────► NODE 9  (not falling - can sprint)
└─ True ──────────────────► Return Node  (falling - can't sprint)

NODE 9: SET bIsSprinting
├─ bIsSprinting ◄─────── TRUE (checkbox)
├─ Exec Out ──────────────► NODE 10

NODE 10: Set Max Walk Speed
├─ Target ◄──────────────── MovementComponent
├─ New Max Walk Speed ◄─── SprintSpeed (GET)
├─ Exec Out ──────────────► Return Node
```

---

## 🔧 Function: StopSprint (Custom Function)

### Схема нод (упрощённая):

```
[ ENTRY ]
    │
    ▼
┌─────────────────────┐
│ IsValid             │
│ (MovementComponent) │
└────────┬────────────┘
         │ True
         ▼
┌─────────────────────┐
│ SET bIsSprinting    │
│ = False             │
└────────┬────────────┘
         │
         ▼
┌─────────────────────┐
│ Set Max Walk Speed  │
│ (WalkSpeed)         │
└────────┬────────────┘
         │
         ▼
     [ RETURN ]
```

---

## 🔧 Event: Tick Component

### Когда использовать: Если нужен расход стамины

### Схема нод:

```
┌──────────────────┐
│ Event Tick       │
│ Component        │
└────────┬─────────┘
         │ Delta Time (float)
         ▼
┌─────────────────────┐
│ Branch              │
│ Condition:          │
│ bIsSprinting (GET)  │
└────────┬────────────┘
         │ True
         ▼
┌─────────────────────┐
│ Get Stamina         │
│ Component           │
└────────┬────────────┘
         │ Optional
         ▼
┌─────────────────────┐
│ Branch: IsValid?    │
└────────┬────────────┘
         │ True
         ▼
┌─────────────────────┐
│ Float * Float       │
│ A: StaminaDrain     │
│ B: Delta Time       │
└────────┬────────────┘
         │ Result
         ▼
┌─────────────────────┐
│ Try Consume Stamina │
│ (Amount: above)     │
└────────┬────────────┘
         │ Bool (success?)
         ▼
┌─────────────────────┐
│ Branch              │
└────────┬────────────┘
         │ False (not enough stamina)
         ▼
┌─────────────────────┐
│ StopSprint          │
│ (function call)     │
└─────────────────────┘
```

---

## 🎨 Blueprint Graph Layout Tips

### Организация графа:

```
┌─────────────────────────────────────────────────────────┐
│                    EVENT GRAPH                          │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  [Events Section - слева]                               │
│  ┌──────────────┐                                       │
│  │ BeginPlay    │                                       │
│  └──────────────┘                                       │
│  ┌──────────────┐                                       │
│  │ Tick         │                                       │
│  └──────────────┘                                       │
│                                                         │
│  ──────────────────────────────────────                 │
│                                                         │
│  [Main Logic - центр]                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ Validation   │→ │  Processing  │→ │   Result     │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
│                                                         │
│  ──────────────────────────────────────                 │
│                                                         │
│  [Comments]                                             │
│  Use Comment boxes to group related nodes               │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### Best Practices для Blueprint графа:

1. **Слева направо**: Execution flow идёт слева направо
2. **Сверху вниз**: Второстепенные ветки идут вниз
3. **Группировка**: Используйте Comment boxes для группировки
4. **Выравнивание**: Выравнивайте ноды по grid
5. **Reroute nodes**: Используйте для длинных соединений

---

## 🔌 Pin Types и их подключение

### Execution Pins (белые):
```
─────► Execution flow (порядок выполнения)
```

### Data Pins (цветные):
```
Boolean (Red):     ─────► true/false
Integer (Cyan):    ─────► целые числа
Float (Green):     ─────► дробные числа
String (Magenta):  ─────► текст
Vector (Yellow):   ─────► 3D координаты
Object (Blue):     ─────► ссылки на объекты
```

### Правила подключения:
- ✅ Execution → Execution (белый к белому)
- ✅ Float → Float (зелёный к зелёному)
- ✅ Float → Integer (с конвертацией)
- ✅ Integer → Float (автоматически)
- ❌ Boolean → Float (нельзя напрямую)
- ✅ Object Reference → Object Reference (того же типа или child)

---

## 📋 Events vs Functions vs Macros

### Events (красные):
```
┌────────────────┐
│ EVENT          │  ← Красный title bar
│ BeginPlay      │
└───────┬────────┘
        │ Только Exec Out (нет Exec In)
```
**Использовать для**: Входных точек (BeginPlay, Tick, Input)

### Functions (фиолетовые):
```
┌────────────────┐
│ FUNCTION       │  ← Фиолетовый title bar
│ StartSprint    │
└───────┬────────┘
        │ Exec In и Exec Out
        │ Можно Return Value
```
**Использовать для**: Переиспользуемой логики, можно вызывать много раз

### Macros (синие):
```
┌────────────────┐
│ MACRO          │  ← Синий title bar
│ CheckValid     │
└───────┬────────┘
        │ Может иметь несколько Exec Outputs
```
**Использовать для**: Collapse логики, несколько выходов

---

## 🎯 Пример полной структуры Blueprint Component

```
BP_SprintComponent
│
├── [Variables]
│   ├─ WalkSpeed (float, 500.0, EditAnywhere)
│   ├─ SprintSpeed (float, 800.0, EditAnywhere)
│   ├─ StaminaDrainRate (float, 20.0, EditAnywhere)
│   ├─ MinStamina (float, 10.0, EditAnywhere)
│   ├─ bIsSprinting (bool, false, BlueprintReadOnly)
│   ├─ OwnerCharacter (ACharacter*, private)
│   ├─ MovementComponent (UCharacterMovement*, private)
│   └─ StaminaComponent (UStaminaComponent*, private)
│
├── [Event Graph]
│   ├─ Event BeginPlay
│   │   └─ Initialize references
│   │
│   └─ Event Tick Component
│       └─ Drain stamina if sprinting
│
├── [Functions]
│   ├─ StartSprint
│   │   ├─ Input: None
│   │   └─ Output: None
│   │
│   ├─ StopSprint
│   │   ├─ Input: None
│   │   └─ Output: None
│   │
│   └─ IsSprinting (Pure)
│       ├─ Input: None
│       └─ Output: bool
│
└── [Events (Custom)]
    └─ OnStaminaDepleted (subscribed from StaminaComponent)
        └─ Call StopSprint
```

---

## 🔗 Integration с персонажем

### В BP_PlayerCharacter:

#### Components panel:
```
Components
├─ CapsuleComponent
├─ Mesh
├─ SpringArm
├─ Camera
└─ SprintComponent (BP_SprintComponent)  ← Add this
```

#### Event Graph - Input:
```
┌──────────────────┐
│ InputAction      │
│ Sprint           │
└────────┬─────────┘
         │ Started
         ▼
┌──────────────────────┐
│ Get Sprint Component │
└────────┬─────────────┘
         │ Component ref
         ▼
┌──────────────────┐
│ StartSprint      │
│ (function)       │
└──────────────────┘


         │ Completed
         ▼
┌──────────────────────┐
│ Get Sprint Component │
└────────┬─────────────┘
         │ Component ref
         ▼
┌──────────────────┐
│ StopSprint       │
│ (function)       │
└──────────────────┘
```

---

## 💡 Tips для создания Blueprint

### 1. Используйте Pure Functions где возможно
```
Pure Function (нет Exec pins):
┌──────────────┐
│ GET Variable │  ← Не имеет execution pins
└──────────────┘  ← Можно вызывать много раз без копий
```

### 2. Избегайте циклических зависимостей
```
❌ ПЛОХО:
Component A → calls → Component B
Component B → calls → Component A

✅ ХОРОШО:
Component A → fires event → Component B listens
```

### 3. Группируйте логику в Functions
```
Вместо:
Event Graph: 200 nodes

Лучше:
Event Graph: 10 nodes (calls functions)
Functions: 10 functions × 20 nodes each
```

### 4. Используйте Local Variables в функциях
```
Function StartSprint:
├─ Local Variables:
│  └─ TempStamina (float)
└─ Logic uses TempStamina (не загрязняет class variables)
```

---

## 🎓 Итого

Этот документ показывает:
- ✅ Как проектировать Blueprint архитектуру
- ✅ Как соединять ноды (execution + data pins)
- ✅ Как организовывать граф
- ✅ Best practices для Blueprint

**Главное**: Blueprint должен быть читаемым как код!
