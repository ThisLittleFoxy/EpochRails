# Примеры модульной архитектуры для EpochRails

## 📁 Файлы в этой папке

### 📖 Документация:
- **ARCHITECTURE_PATTERNS.md** - Полное руководство по всем архитектурным паттернам
  - ActorComponent
  - Interfaces
  - Gameplay Ability System
  - Subsystems
  - Data-Driven подход
  - Сравнительная таблица

### 💻 Примеры кода:

#### 1. Простые компоненты:
- **SprintComponent.h/cpp** - Компонент для спринта (базовый пример)
- **JumpControlComponent.h** - Компонент для прыжков с двойным прыжком и dash

#### 2. Модульный персонаж:
- **ModularCharacterExample.h/cpp** - Персонаж с компонентной архитектурой
  - Показывает как персонаж становится "контейнером" компонентов
  - Минимум логики - все делегируется в компоненты

#### 3. Взаимодействие между компонентами:
- **ComponentInteractionExample.h/cpp** - Как компоненты общаются через события
  - StaminaComponent (энергия)
  - SprintComponent (использует стамину)
  - JumpComponent (использует стамину)
  - UIComponent (отображает стамину)
  - Показывает систему делегатов и событий

---

## 🎯 Главная идея

### Было:
```cpp
class Character {
  // 1000+ строк логики спринта, прыжков, взаимодействия, боя...
}
```

### Стало:
```cpp
class Character {
  USprintComponent *Sprint;
  UJumpComponent *Jump;
  UCombatComponent *Combat;

  void OnSprintPressed() { Sprint->Start(); }
}
```

---

## ✅ Преимущества модульного подхода

1. **Изолированность** - изменения в одном компоненте не влияют на другие
2. **Переиспользование** - один компонент можно использовать на разных персонажах
3. **Легко добавлять** - новая механика = новый компонент
4. **Легко убирать** - просто удалите компонент
5. **Blueprint friendly** - компоненты можно менять в Blueprint
6. **Легко тестировать** - каждый компонент тестируется отдельно
7. **Чистый код** - каждая система в отдельном файле

---

## 🚀 Как начать использовать

### Шаг 1: Определите системы
Какая логика есть в вашем персонаже?
- Movement (Sprint, Crouch, Climb)
- Jump (Single, Double, Dash)
- Combat (Attack, Block, Dodge)
- Interaction (Use, Pickup, Talk)

### Шаг 2: Создайте компонент для каждой системы
```cpp
UCLASS(BlueprintSpawnableComponent)
class UMyFeatureComponent : public UActorComponent {
  GENERATED_BODY()
  // Вся логика системы здесь
};
```

### Шаг 3: Добавьте компонент в персонажа
```cpp
UPROPERTY(VisibleAnywhere, BlueprintReadOnly)
UMyFeatureComponent *FeatureComponent;
```

### Шаг 4: Делегируйте вызовы
```cpp
void ACharacter::OnFeaturePressed() {
  if (FeatureComponent) {
    FeatureComponent->DoFeature();
  }
}
```

---

## 📚 Что читать дальше

1. **ARCHITECTURE_PATTERNS.md** - Полный обзор всех паттернов
2. **SprintComponent.h/cpp** - Простой пример для начала
3. **ComponentInteractionExample.h/cpp** - Продвинутые техники взаимодействия

---

## 💡 Примеры из вашего проекта

Вы **уже используете** компонентный подход:

### ✅ UInteractionComponent
- Отвечает за взаимодействие с объектами
- Line trace, фокус на объектах, UI взаимодействие
- Персонаж просто вызывает `TryInteract()`

### ✅ USpringArmComponent
- Управление камерой
- Персонаж не знает деталей - компонент всё делает сам

### ✅ UCameraComponent
- Камера персонажа
- Отдельный компонент

**Вы можете применить тот же подход к любой другой логике!**

---

## 🔄 Взаимодействие компонентов

Компоненты могут общаться между собой через:

### 1. События (Delegates):
```cpp
// Компонент А
DECLARE_DYNAMIC_MULTICAST_DELEGATE(FOnSomethingHappened);
UPROPERTY(BlueprintAssignable)
FOnSomethingHappened OnSomethingHappened;

// Компонент B подписывается
ComponentA->OnSomethingHappened.AddDynamic(this, &UComponentB::HandleEvent);
```

### 2. Поиск других компонентов:
```cpp
// Динамически находим другой компонент
UStaminaComponent *Stamina = GetOwner()->FindComponentByClass<UStaminaComponent>();
if (Stamina) {
  Stamina->ConsumeStamina(10.0f);
}
```

### 3. Через персонажа (геттеры):
```cpp
// В персонаже
UStaminaComponent *GetStaminaComponent() const { return StaminaComponent; }

// В компоненте
AMyCharacter *Owner = Cast<AMyCharacter>(GetOwner());
if (Owner && Owner->GetStaminaComponent()) {
  // ...
}
```

---

## ⚠️ Важные моменты

### ✅ DO (делайте):
- Используйте `FindComponentByClass` для гибкости
- Подписывайтесь на события других компонентов
- Делайте компоненты независимыми (работают даже если других нет)
- Используйте UPROPERTY для кэширования ссылок

### ❌ DON'T (не делайте):
- Не храните жесткие ссылки между компонентами в заголовках
- Не делайте компоненты зависимыми друг от друга
- Не дублируйте логику в персонаже и компоненте
- Не забывайте проверять nullptr перед использованием

---

## 🎓 Дополнительные ресурсы

- [Unreal Engine: Actor Components](https://docs.unrealengine.com/5.3/en-US/components-in-unreal-engine/)
- [Component Lifecycle](https://docs.unrealengine.com/5.3/en-US/actor-lifecycle-in-unreal-engine/)
- [Delegates and Events](https://docs.unrealengine.com/5.3/en-US/delegates-and-events-in-unreal-engine/)

---

## 📞 Вопросы?

Если что-то непонятно - посмотрите примеры кода в этой папке.
Все примеры содержат подробные комментарии и объяснения.

**Главное правило**: Персонаж не должен содержать игровую логику.
Он только координирует компоненты! 🎯
